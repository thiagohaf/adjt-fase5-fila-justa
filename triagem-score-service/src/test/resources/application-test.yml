spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/confirma_sus_test?currentSchema=triagem_score
    username: confirma_sus_user
    password: confirma_sus_password
  flyway:
    clean-on-validation-error: true
