/**
 * Camada de dominio do gateway-service -- sem dependencia de framework
 * (AD-2). Vazia nesta story: o gateway ainda nao possui regra de dominio
 * propria (validacao de JWT/roteamento entram na Story 1.2, AD-8).
 */
package com.filajusta.gateway.domain;
