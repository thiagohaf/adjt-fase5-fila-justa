/**
 * Casos de uso do gateway-service (AD-2). Vazia: a validacao de JWT (Story
 * 1.2, AD-8) e um cross-cutting concern do Gateway ({@code GlobalFilter}),
 * nao um caso de uso -- vive inteira em
 * {@code infrastructure.security.JwtAuthenticationFilter}.
 */
package com.filajusta.gateway.application;
